/* **This is a test file
   *
   *
   *
   for checking the lexical analyzer created By 14CS30032**
*/


#include <stdio.h>


int main(){ // main function//

	// list of keywords
	auto 	enum 	restrict 	unsigned
	break 	extern 	return 		void
	case 	float 	short 		volatile
	char 	for 	signed 		while
	const 	goto 	sizeof 		_Bool
	continue if 	static 		_Complex
	default inline 	struct 		_Imaginary
	do 	int 	switch
	double 	long 	typedef
	else 	register union
	
	//check for identifiers
	int _mn074sds = 7;
	float Flaot_451_jerk_noob = 234.24;
	
	//checks for constants
	int x = 50124;
	float f1 = 235.658;
	float f2 = .5985;
	float f3 = 35454.
	float f4 = 14.5e-43;
	float f5 = .324E23;
	float f6 = 64456e+63;
	//character constant
	'lnds\nqssa'	
	'ksjdbsk\'sjjs'
	//string literals
	""
	"355s4as4a5a\\"
	"adnjnkna\aasbanskl"
	
	//puctuator checks
	int y = 4+6-2<< 4.3%1>>23/4;
	float z = (x==y)?6:22.4
	y += 54E3;
	p[2] *= (x++)*(y->z)...&qw^~q;
	if(!a || b && c== d != e >= f <= g > h < i)  j^=k|=l
	func(p, q, r)
	return 0;
}

















